# Benchmarks

Dated latency results for the CPU reference backend, generated by the
reproducible harness in `portable_attention.benchmark`. Each block carries
hardware, software, and commit context and is appended (newest first).

Regenerate a block with:

```sh
python -m portable_attention.benchmark --threads 1 --commit "$(git rev-parse --short HEAD)"
```

## Reading these numbers: cap BLAS threads for multi-head work

Multi-head attention issues many small per-slice GEMMs through NumPy's batched
`matmul`. Under the default OpenBLAS policy (one thread per core) the
thread-synchronization overhead of those tiny GEMMs dominates, so latency can be
**15–25× worse** than with a single BLAS thread (issue #8). Until the planned
blocked/streaming CPU kernel batches the head dimension into fewer, larger
GEMMs, the practical guidance is to cap BLAS threads for multi-head workloads:

```sh
OPENBLAS_NUM_THREADS=1 python your_script.py
```

The harness pins the thread count for every measurement (via `threadpoolctl`
when installed) and records the process-default configuration in each block, so
the headline numbers reproduce. The thread sweep in each block keeps the
default-threading cliff visible.

Hardware floor for this project: a low-power ARM board (aarch64, 4× Cortex-A76,
8 GB RAM, no discrete GPU).

---

## 2026-07-25 — commit 2f94b7b

**Environment:** aarch64, 4 CPUs. Python 3.12.13, NumPy 2.5.1 (scipy-openblas
0.3.33, DYNAMIC_ARCH). portable-attention 0.0.1. `threadpoolctl` 3.6.0.

**BLAS threads (process default, via threadpoolctl):** openblas=4. Each row
below is measured with the thread count pinned to its `threads` value.

### Latency by shape (pinned threads, median over 20 repeats)

| shape (B,H,S,D)   | dtype   | threads | latency (median) |
|-------------------|---------|---------|-----------------:|
| (1, 1, 64, 32)    | float32 | 1       |         0.183 ms |
| (1, 8, 128, 64)   | float32 | 1       |         5.912 ms |
| (2, 8, 256, 64)   | float32 | 1       |        49.961 ms |
| (1, 12, 512, 64)  | float32 | 1       |       139.326 ms |

### Thread sweep (multi-head shape — the cliff stays visible)

| shape (B,H,S,D)   | dtype   | threads | latency (median) |
|-------------------|---------|---------|-----------------:|
| (1, 8, 128, 64)   | float32 | 1       |         5.284 ms |
| (1, 8, 128, 64)   | float32 | default |       116.965 ms |

The default (4-thread) run of the multi-head shape is ~22× slower than the
single-thread run — the cliff described above. This is the first block from the
reproducible harness; it supersedes the earlier ad-hoc measurements.
